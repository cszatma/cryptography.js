{
  "name": "cryptography",
  "version": "1.0.0",
  "main": "build/index.js",
  "bin": "bin/crypto.js",
  "private": true,
  "dependencies": {
    "seedrandom": "^2.4.3"
  },
  "devDependencies": {
    "@types/node": "^10.3.2",
    "@types/seedrandom": "^2.4.27",
    "husky": "^0.14.3",
    "lint-staged": "^7.1.3",
    "prettier": "^1.13.5",
    "rimraf": "^2.6.2",
    "ts-node": "^6.1.0",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.13.0",
    "tslint-plugin-prettier": "^1.3.0",
    "typescript": "^2.9.1"
  },
  "scripts": {
    "build": "rimraf build/ && tsc",
    "crypto": "ts-node src/crypto.ts",
    "lint": "tslint --fix",
    "precommit": "lint-staged",
    "watch": "tsc -w"
  },
  "prettier": {
    "trailingComma": "all",
    "singleQuote": true,
    "tabWidth": 2
  },
  "lint-staged": {
    "*.ts": [
      "tslint -p ./tsconfig.json --fix",
      "git add"
    ],
    "*.{js,json}": [
      "prettier --write",
      "git add"
    ]
  }
}
